namespace DIPHua {
    public enum HColor {
        R = 2, G = 1, B = 1
    }
    public enum SimpleFilter {
        ArithmeticMean, Median, Max, Min, MidPoint
    }
    public enum ImagePosition {
        LeftTop, RightTop, RightDown, LeftDown
    }
    public enum GaussianPass {
        Low = 0, High = 1
    }
    public enum BinaryImage {
        Zero = 0, False = 1024
    }
}
